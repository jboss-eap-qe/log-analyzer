// import TextViewer from 'js/lib//text-viewer';
// import WindowManager from 'js/lib/window-manager';
// import $ from 'jquery';
//
// let textViewer1 = new TextViewer(192058, 500);
// let textViewer2 = new TextViewer(192058, 500);
// let windowManager = new WindowManager($('#window-manager'));
// windowManager.openWindow('console', textViewer1.getContainer());
// windowManager.openWindow('server-1.trace', textViewer2.getContainer());
//
// textViewer1.recalculate();
// textViewer2.recalculate();

import index from 'js/index';

window.index = index;
